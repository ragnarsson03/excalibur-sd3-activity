<%- include('partials/header') %>

    <div class="flex flex-col items-center justify-center min-h-[60vh] text-center px-4">
        <div class="w-32 h-32 bg-red-50 rounded-full flex items-center justify-center mb-8 relative">
            <div class="absolute inset-0 bg-red-100 rounded-full animate-ping opacity-20"></div>
            <i class="ph-fill ph-warning-circle text-6xl text-red-500 relative z-10"></i>
        </div>

        <h1 class="text-6xl font-black text-slate-900 tracking-tight mb-4">404</h1>
        <h2 class="text-2xl font-bold text-slate-700 mb-6">Página no encontrada</h2>

        <p class="text-slate-500 max-w-md mx-auto mb-8 text-lg">
            Lo sentimos, la página que estás buscando no existe, ha sido movida o está temporalmente inactiva.
        </p>

        <a href="/"
            class="btn btn-primary rounded-full px-8 py-3 text-base shadow-lg shadow-brand/30 hover:-translate-y-1 transition-transform">
            <i class="ph ph-house"></i> Volver al Inicio
        </a>
    </div>

    <%- include('partials/footer') %>