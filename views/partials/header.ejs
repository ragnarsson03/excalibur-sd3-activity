<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Gestión — Fundación 100% San Agustín</title>
    <meta name="description"
        content="Sistema web de gestión de beneficiarios, actividades, voluntarios y recursos de la Fundación 100% San Agustín.">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        :root {
            --primary: #ff5722;
            --primary-dark: #e64a19;
        }

        body {
            background-color: #f4f6f8;
        }

        .navbar-brand {
            font-weight: 800;
            color: var(--primary) !important;
            font-size: 1.3rem;
        }

        .btn-primary {
            background-color: var(--primary);
            border-color: var(--primary);
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            border-color: var(--primary-dark);
        }

        .nav-link:hover {
            color: var(--primary) !important;
        }

        .table thead {
            background: #343a40;
            color: #fff;
        }

        .card {
            border: none;
            border-radius: 12px;
        }

        .card-header {
            border-radius: 12px 12px 0 0 !important;
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/"><i class="bi bi-house-heart-fill"></i> Fundación 100% San Agustín</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/beneficiarios"><i class="bi bi-people-fill"></i> Beneficiarios</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/actividades"><i class="bi bi-calendar-event-fill"></i>
                            Actividades</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/voluntarios"><i class="bi bi-person-badge-fill"></i> Voluntarios</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/recursos"><i class="bi bi-box-seam-fill"></i> Recursos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/asistencias"><i class="bi bi-clipboard2-check-fill"></i>
                            Asistencias</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container pb-5">