# ──────────────────────────────────────────────────────────────
# Archivo de ejemplo para variables de entorno
# Copia este archivo como .env y rellena los valores reales
# NUNCA subas el .env real a GitHub
# ──────────────────────────────────────────────────────────────

# Conexión a MongoDB Atlas (el nombre DEBE ser exactamente MONGODB_URI)
MONGODB_URI=mongodb+srv://<usuario>:<password>@cluster.mongodb.net/<nombre_base_datos>?retryWrites=true&w=majority

# Puerto del servidor
PORT=3000
